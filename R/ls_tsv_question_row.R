#' @export
ls_tsv_question_row <- function(questioncode,
                                text,
                                id=NULL,
                                class = "Q",
                                type.scale = "L",
                                relevance = 1,
                                language = "en",
                                mandatory = "N",
                                other = "N",
                                same_default = 1,
                                cssclass = "",
                                hide_tip = 1) {
  if (is.null(id)) {

  }
  res <- as.list(environment());

}
